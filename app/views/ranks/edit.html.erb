<div class="layui-tab">
  <div class="x-nav">
    <span class="layui-breadcrumb">
      <a href="">内容管理</a>
      <%= link_to @grade.name, grade_ranks_path(@grade) %>
      <a><cite><%= @rank.name %></cite></a>
      <a><cite>修改排行榜 </cite></a>
    </span>
    <%= render 'common/user_info' %>
  </div>

  <div class="x-body">
    <%= render 'form' %>
  </div>
</div>
